node_modules
package-lock.json
# Tutte le varianti (.env, .env.local, .env.produzione…) contengono credenziali
# dei due database. Prima era ignorato solo `.env`, quindi `.env.local` — il file
# effettivamente in uso — era a un `git add .` dal finire nella history.
.env*
!.env.example
*.log

# Report di migra-allegati.js: elencano hash e percorsi degli allegati
report-allegati-*.csv
orfani-allegati-*.txt
