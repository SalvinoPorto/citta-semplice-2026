# Usato da prisma.config.ts per gli script db:push / db:migrate / db:studio.
# Copiare in packages/db/.env con l'URL del database locale (lo stesso valore
# usato in citta-semplice-office/.env e citta-semplice-portal/.env).
DATABASE_URL="postgresql://user:password@localhost:5432/citta_semplice?schema=public"
